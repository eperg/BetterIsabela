.DS_Store
node_modules/
dist/
.next/
react-app/out/
out/
*.backup

# Playwright
test-results/
playwright-report/
blob-report/
.playwright/

# Build-time config (see .env.example) and release archives
.env
release/

# Output of `npm run build:verify`, which builds to a separate dir so it
# cannot clobber a running `next dev`.
.next-build/

# Local PGlite dev database (npm run db:serve). Binary Postgres data,
# regenerated by `npm run db:migrate && npm run db:demo`.
.pglite/

# TypeScript incremental build state
*.tsbuildinfo
.vercel
.env*
!.env.example
